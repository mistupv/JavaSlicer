public class TestJosep {

    public static void main(int y) {
        int z = 0;
        while (y > z) {
            y--;
        }
        switch(z) {
            default:
                break;
            case 3:
                y = x + z;
        }
        log(y);
    }
}
