class A {

    int xA = 0;

    A(int newx) {
        xA = newx;
    }

    int getx() {
        return xA;
    }

    public static void main(String[] args) {
        A a1 = new A(1);
        int z = a1.getx();
        System.out.println(z);
    }
}
