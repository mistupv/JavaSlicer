class A {

    int getx() {
        return xA;
    }

    void setx(int newx) {
        xA = newx;
    }

    void siModificaxA(int v) {
        xA++;
    }

    public static void main(String[] args) {
        A a1 = new A();
        a1.setx(5);
        a1.siModificaxA(5);
        int z = a1.getx();
        System.out.println(z);
    }
}
